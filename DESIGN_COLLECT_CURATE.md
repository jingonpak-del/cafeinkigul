# 설계안 — 전량 수집(Lake) + 큐레이션 (검토용)

> 방향 전환: **"지정 게시판만 수집→인기 비교"** → **"일단 다 크롤해 자료를 쌓고(Lake),
> 그 위에서 필요한 것을 큐레이션"**. 축적을 기본 베이스로, 큐레이션을 상위 레이어로.
> 상태: **검토 대기.** (코드가 fork에서 활발히 병합 중 — 이 계획은 그 위에 정렬)

---

## 0. 이미 있는 것 (인벤토리 — 새로 만들 필요 없음)
| 파일 | 역할 | 비고 |
|---|---|---|
| `backfill.py` | **과거글 전량 크롤**(article_id 역스캔, 카페별 DB커서 재개, 2년 경계) | lane="backfill" |
| `raw_archive.py` | content_html → zstd 아카이브(DB에서 분리, 압축 10.5배) | DB 920MB 문제 해결 |
| `paths.py` | 데이터 `D:\cafe-corpus`(DB·raw·logs). 코드는 C: | server/watcher/cli 모두 이 DB 사용(통일됨) |
| `ratelimit.py` | 계정 전역 요청 예산(stream 우선/backfill 양보, 서킷) | 스트림·백필 합산 레이트 관리 |
| `idstore.py` | **다계정** 브릿지(`아이디관리` 단일 소유, ~50계정) | 밴·레이트 분산의 핵심 |
| `usage.py`/`keepalive.py` | 요청량 이력 / 세션 유지 | |
| `db.py` | `lane`(stream/backfill), `upsert_article_from_body`, save_body→아카이브 | |
| `discovery.py`+`cafe_candidates` | 섹션 발굴·후보(회원수·대표/동네·주제, `probed_at`) | 이번에 추가·fork가 확장 |

**결론:** 수집·축적 엔진은 존재. **빠진 건 (1) 발굴→크롤 자동확장, (2) 상시 전량(all-board)
포착, (3) 큐레이션 지능(가치판단·동적 카테고리).**

---

## 1. 목표 아키텍처 — 2계층 분리
- **수집(Lake):** 등록된 모든 카페의 **모든 게시판** 글을 다 모은다. **분류는 수집의 전제가 아님**
  (대부분 '미분류'로 쌓임). 원본 텍스트+아카이브.
- **큐레이션(표시):** 규칙/라벨로 Lake의 부분집합을 뽑아 카테고리 탭에 노출. 카테고리는
  **쿼리 시점 라벨**(현재 board→category 방식 확장) → 재라벨링에 재크롤 불필요.

> 지금 대시보드(지정 게시판 인기 비교)는 **큐레이션의 한 뷰**로 그대로 유지되고,
> 그 아래에 "전체(Lake)/미분류" 뷰와 동적 카테고리 뷰가 붙는다.

---

## 2. 갭 & 단계별 계획

### Phase 1 — 발굴 → 크롤 자동확장 (핵심 연결)
- **`crawl_all` 카페 모드:** 후보를 등록할 때 게시판을 일일이 안 골라도
  **① 인기글 무조건 포함 ② 그 카페 전 게시판을 크롤 대상**으로.
- 등록 시 동작: config.cafes에 `{cluburl, club_id, crawl_all:true, boards:[{popular}]}` 추가
  → **backfill 커서 자동 생성**(과거) + 스트림/헤드전진(신규, Phase 2).
- 발굴 탭에 **[통째 추가]** 버튼(게시판 선택 없이 즉시 crawl_all 등록). 기존 [등록](게시판 선별)도 유지.
- 자동 채택 옵션(선택): 점수 상위 후보를 매일 자동 crawl_all 등록(상한 N/일).

### Phase 2 — 상시 전량 수집 (신규글 all-board 포착)
현재 스트림은 **지정 게시판만** 폴링 → 비지정 게시판 신규글은 backfill(과거)만 잡음. 방식 택1:
- **(a) 헤드 전진 backfill(권장):** article_id 단조증가 이용, 주기적으로 `(직전 head, 현재 head]`
  구간만 스캔 → 전 게시판 신규글을 저비용으로 포착. 실시간성↓·부하↓.
- (b) all-board 스트림: crawl_all 카페의 전 게시판을 라운드로빈 폴링. 실시간성↑·부하↑↑.
- (c) 하이브리드: 인기글+대표 게시판만 실시간 스트림, 나머지는 (a).
> 권장: **(c)** — 인기글/핵심은 실시간, 전량은 헤드전진으로.

### Phase 3 — 큐레이션 분석 (가치 판단)
- **`board_stats`(카페·게시판별):** 글 수, 인기글 진입 수, 평균 반응, 최근 인기 시각
  → **가치 있는 게시판** 자동 식별.
- **주제 신호:** 카페 theme + 게시판명 + 제목 키워드 집계(후에 임베딩/LLM) → **떠오르는 주제** 탐지.
- **제안 큐(master):** "이 게시판이 인기글 다수 배출 → 등록/카테고리화 추천",
  "운동 카페 다수 + 대회정보 인기 → **대회정보 카테고리 신설 추천**".

### Phase 4 — 동적 카테고리 (라우팅)
- 카테고리 배정 = **규칙**: board→cat, keyword→cat, cafe theme→cat (쿼리 시 적용, 재크롤 X).
- 새 카테고리는 master가 신설(기존 추가 흐름) + 라우팅 규칙 등록 → 예: 대회정보.
- 대시보드 탭이 동적으로 늘어남(이미 동적).

### Phase 5 — (후속) LLM 큐레이션/재구성
- 주제 클러스터링·요약, 재게시 원고 생성(기존 `studio`·딜 파이프라인과 연계).

---

## 3. 규모·리스크 (반드시 반영)
- **계정 밴:** 단일 계정 대량 크롤은 정지 위험. → **다계정(idstore) 분산 + ratelimit** 필수.
  crawl_all 확대 전에 계정 배분 정책 확정.
- **가입필요 카페:** 회원제 카페는 크롤 계정이 **가입해야** 인기글·글이 보임. 자동가입은 안 함
  (CAPTCHA·약관) → `join_required` 표시 + 사람이 가입(다계정 활용). '가입 자동화 fork'는 이 지점 보조.
- **저장 폭증:** 이미 raw 아카이브로 완화. + **보존정책**(텍스트 영구 / 원본 아카이브 콜드이관) 필요.
- **저작권·PII:** 재게시(Phase 5) 시 원문 표절·개인정보 리스크 → 재구성·출처제거·검수.

---

## 4. 지금 정해야 할 것 (검토 포인트)
1. **상시 전량 방식**: (a)헤드전진 / (b)all-board 스트림 / (c)하이브리드[권장] 중?
2. **최종 목적 우선순위**: 재구성·재게시용 큐레이션 / 학습 코퍼스(10억 토큰) / 둘 다?
   → 보존·PII·볼륨 정책이 달라짐.
3. **발굴 자동 채택 범위**: 후보를 (수동 승인만) / (점수상위 자동 crawl_all) / (전량 자동)?
4. **다계정 크롤 분산**: 지금 도입(밴 회피) vs 단일 계정으로 시작 후 확장?
5. 이 문서 + 앞선 두 설계문서 커밋 여부.

---

## 5. 권장 착수 순서
1. **Phase 1**(발굴→crawl_all 통째 추가 + 인기글 무조건 + backfill 커서 자동) — 즉시 체감, 위험 낮음.
2. **Phase 2(c)** 헤드전진으로 상시 전량 포착.
3. **Phase 3** board_stats·제안 큐(큐레이션 지능의 시작).
4. **Phase 4** 동적 카테고리 라우팅.
5. Phase 5는 앞선 딜/스튜디오 설계와 합류.
