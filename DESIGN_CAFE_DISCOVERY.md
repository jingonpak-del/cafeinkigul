# 설계안 — 카페 발굴·큐레이션 모듈 (검토용)

> 목적: 수집 **대상 카페를 지속적으로 넓힌다.** 매일 섹션 페이지에서 참조할 만한
> 후보 카페를 찾아 **판단 목록(지표)** 으로 보여주고, 체크 한 번으로 크롤링 대상에
> 편입한다. 다양한 주제의 카페·콘텐츠를 최대한 확보하는 게 현재 목표.
> (딜 파이프라인 = 수집한 콘텐츠 *활용* / 이 문서 = 수집 대상 *확장*. 별개 모듈.)
> 상태: **검토 대기.** 승인 후 구현.

## 발굴 소스 (매일 조사)
- 테마별: `section.cafe.naver.com/ca-fe/home/themes/{n}?type=ar`
- 지역별(동네카페): `.../home/areas`
- 파워/대표카페: `.../home/powers/popular`
- (선택) 키워드 검색: 우리가 아직 못 덮은 주제어로 카페 검색

> ⚠️ **구현 0단계 — 엔드포인트 캡처(필수):** 위 페이지는 SPA라 화면 뒤의 JSON API
> (`apis.naver.com/...`)를 호출한다. **제 환경은 naver 접근이 정책 차단**되어 확인
> 불가 → 사장님 로그인 브라우저의 개발자도구(Network)에서 목록/카페정보 API 주소·
> 파라미터를 함께 캡처해 `discovery.py`에 넣는다. 이후 단계는 이 API 확보를 전제로 함.

---

## 데이터 모델 (db.py, 신규 테이블)
```sql
CREATE TABLE IF NOT EXISTS cafe_candidates (
    club_id       INTEGER PRIMARY KEY,
    cluburl       TEXT,
    name          TEXT,
    source        TEXT,        -- theme:2 | area:<code> | power | search:<kw>
    theme         TEXT,        -- 대표주제(섹션 테마/카테고리명)
    is_power      INTEGER,     -- 대표(파워)카페 여부 1/0
    is_local      INTEGER,     -- 동네(지역)카페 여부 1/0
    member_count  INTEGER,     -- 회원수
    daily_posts   INTEGER,     -- 하루 발행량(최근 글 타임스탬프로 추정)
    open_level    TEXT,        -- 공개 | 비공개 | 부분공개
    join_required INTEGER,     -- 인기글/글 열람에 가입 필요 1/0
    sample_boards TEXT,        -- 대표 게시판명 몇 개(콤마)
    score         REAL,        -- 참조가치 점수(정렬용)
    discovered_at INTEGER,
    status        TEXT DEFAULT 'new'   -- new | tracked | dismissed | join_needed
);
```
> 이미 등록된 카페(config)·`dismissed`·`tracked`는 재노출하지 않음(중복 방지).

---

## 판단 목록에 뽑는 지표 (요청 항목 반영)
| 컬럼 | 의미 | 취득 방법 |
|---|---|---|
| 카페명/대표주제 | 무슨 카페인지 | 섹션 목록 + 카페정보 API |
| 대표카페 여부 | 파워카페 뱃지 | powers 소스 or 카페정보 flag |
| 동네카페 여부 | 지역 기반 | areas 소스 or 지역정보 |
| 회원수 | 규모 | 카페정보 API `memberCount` |
| 하루 발행량 | 활동성 | 최근 N개 글 write_ts 간격으로 추정 |
| 공개/가입필요 | 크롤 가능성 | 인기글 목록 미인증→인증 시도 결과 |
| 대표 게시판 | 어떤 판이 있나 | menus API(기존 `cafe-boards`) 상위 몇 개 |

---

## 일일 발굴 잡 (watcher.py 스케줄러)
- 기존 인기글 수집(2/16시)처럼 **하루 1회** `discover_step()` 실행.
- 흐름:
  1. 3개 섹션(+키워드)에서 카페 후보 목록 수집.
  2. 신규(미등록·미dismiss) 카페마다 카페정보/활동성/가입필요 조사.
  3. **참조가치 점수**로 정렬해 상위 **약 5건**만 `status=new`로 적재(하루 큐레이션 상한).
- **점수(참조가치)** = 회원수·일발행량(활동성) + **아직 안 덮은 주제 가중** − 이미 유사 카페 보유.
  → 다양성(새 주제) 우선. 가중치는 config로 조정.
- 부하 억제: 딜 파이프라인과 동일 원칙(호스트당 저율, 사이클당 소수, 실패 무시).

---

## 설정 화면 — 신규 탭 "카페 발굴" (index.html)
기존 ⚙️ 설정(master 전용) 안에 탭 추가:
- **후보 표**: 위 지표 컬럼 + 뱃지(⭐대표 / 📍동네 / 🔒가입필요) + `score`순 정렬.
- **행별 체크박스**:
  - 체크 → **기존 카페 등록 편집기 자동 오픈**(pre-filled). 즉 `/api/admin/cafe-boards?cafe=<cluburl>`
    호출로 게시판 자동 추출 → 사장님이 분류 지정 → 저장(`/api/admin/save-cafe`).
    저장되면 후보 `status=tracked`로 이동, 목록에서 내려감.
  - 인기글 보드는 기본 포함 → **메인화면 인기글 영역에 자동 반영**(추가 작업 불필요).
- **✕ 무시**: `status=dismissed` (다시 안 뜸).
- **🔄 지금 발굴**: 수동 트리거(잡을 즉시 1회 실행).

### 🔒 가입필요 카페 처리 (요청 반영)
- 발굴/등록 시 인기글·목록 조회가 **가입 필요**로 막히면 `join_required=1`,
  `status=join_needed`.
- UI에 명확히 표시 + **가입 요청**:
  > "이 카페는 가입해야 인기글이 보입니다. **크롤링 계정으로 가입 후** [가입 페이지 열기] →
  > 가입 완료하면 [가입완료] 클릭."
- **자동 가입은 하지 않음**(계정 가입·CAPTCHA는 사람이 수행). 가입 URL만 제공.
- [가입완료] 클릭 → 인증 클라이언트로 재확인 → 통과 시 등록 가능 상태로 전환.

---

## 동적 분류 큐레이션 (요청 반영)
- 현재 분류: 핫딜/쇼핑정보 · 앱테크/이벤트 · 유머/볼거리 · 일반인기글.
- 발굴 잡이 후보들의 `theme`를 집계 → **"새로 뜨는 주제" 제안 패널** 표시
  (예: 여행/맛집/육아/재테크…). 사장님이 클릭하면 **기존 분류 추가 흐름**으로 등록.
- 새 분류가 생기면 게시판 등록 시 그 분류를 바로 배정 가능.
  → "주기적으로 조사하며 분류를 뽑아 큐레이션" 요구 충족.

---

## 신규 API (server.py, 모두 master 전용)
- `GET  /api/admin/candidates?status=new` — 후보 목록.
- `POST /api/admin/candidates/refresh` — 지금 발굴(수동).
- `POST /api/admin/candidates/dismiss` `{club_id}` — 무시.
- `POST /api/admin/candidates/{club_id}/join-confirmed` — 가입완료 재확인.
- 등록은 **기존** `/api/admin/cafe-boards`, `/api/admin/save-cafe` 재사용.

---

## 구현 순서
| 단계 | 내용 | 선행 |
|---|---|---|
| 0 | **섹션 API 캡처**(사장님 브라우저에서 함께) | — |
| 1 | `cafe_candidates` 테이블 + `discovery.py`(목록 수집·카페정보·활동성) | 0 |
| 2 | 일일 잡(스케줄러) + 점수·다양성 선별(5건) | 1 |
| 3 | 설정 "카페 발굴" 탭 + 체크→기존 등록편집기 연결 | 2 |
| 4 | 가입필요 감지 + 가입요청 UI + 가입완료 재확인 | 3 |
| 5 | 새 주제 제안(동적 분류 큐레이션) | 3 |

메인화면 인기글 반영은 3단계에서 **자동으로 따라옴**(등록 카페의 popular 보드).

---

## 검토 포인트 (의견 주세요)
1. **하루 후보 5건 자동 선별** 방식 OK? (점수식·다양성 가중 세부는 구현 때 조정)
2. 발굴 소스 우선순위: 파워/대표 · 동네 · 테마 중 **먼저 붙일 것**?
3. 체크 시 동작: (a) **자동으로 등록 편집기 열어 분류 지정 후 저장**(제안) vs
   (b) 즉시 전 보드 크롤 시작 후 나중에 분류.
4. 가입필요 카페: 크롤링에 쓸 **가입용 계정**을 새로 팔지 / 기존 계정 재사용할지.
5. 새 주제 제안 방식: 규칙(테마명 집계)만으로 vs 소수 LLM 요약.
6. 이 문서 커밋 여부.
